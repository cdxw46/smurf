# Hola Mundo · Static site

Pequeña web estática (HTML + CSS + JS) desplegada en Vercel desde Cursor mediante el MCP de Vercel.

## Estructura

- `index.html` – página principal
- `styles.css` – estilos
- `script.js` – contador interactivo
- `vercel.json` – configuración de Vercel
