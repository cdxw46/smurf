# AGENTS.md

## Cursor Cloud specific instructions

This repository (`main` branch) is essentially empty. It contains a single file (`asd`) with no application code, no dependency manifests, no build system, no tests, and no linting configuration.

- **No services to run.** There are no applications, APIs, or frontends on `main`.
- **No dependencies to install.** There is no `package.json`, `requirements.txt`, `pyproject.toml`, or any other manifest.
- **No tests or linting.** No test frameworks or linters are configured.
- **Remote branches** are unrelated CTF challenge experiments and are not part of the main product.

If application code is added in the future, update this file and the VM update script accordingly.
